<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MordheimLedgerApp.Features.Library.EquipmentItems.EquipmentItemView"
             xmlns:models="clr-namespace:MordheimLedgerApp.Core.Models.Library;assembly=MordheimLedgerApp.Core"
             xmlns:icons="clr-namespace:MordheimLedgerApp.Resources.Icons"
             xmlns:loc="clr-namespace:MordheimLedgerApp.Extensions"
             xmlns:components="clr-namespace:MordheimLedgerApp.Components">

    <Grid Margin="5" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- Filtre par catégorie (bouton + ActionSheet), même mécanisme que LibraryTrackView de DmTools -->
        <Grid Grid.Row="0" ColumnDefinitions="*,auto" Padding="5,0">
            <Button Grid.Column="0"
                    Text="{Binding SelectedCategoryLabel}"
                    Command="{Binding SelectCategoryCommand}"
                    BackgroundColor="Transparent"
                    TextColor="{DynamicResource AppAccent}"
                    HorizontalOptions="Start"
                    Padding="0"/>
        </Grid>

        <ActivityIndicator Grid.Row="1" IsRunning="{Binding Loading.IsLoading}" IsVisible="{Binding Loading.IsLoading}"
                           VerticalOptions="Center" HorizontalOptions="Center"
                           WidthRequest="120" HeightRequest="120"/>

        <CollectionView Grid.Row="1" x:Name="ItemsCollection"
                ItemsSource="{Binding EquipmentItems}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedEquipmentItem}"
                ItemSizingStrategy="MeasureFirstItem">
            <CollectionView.Behaviors>
                <components:ResponsiveGridSpanBehavior TileWidth="140"/>
            </CollectionView.Behaviors>
            <CollectionView.ItemsLayout>
                <GridItemsLayout Span="3" VerticalItemSpacing="5" HorizontalItemSpacing="5" Orientation="Vertical"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:EquipmentItem">
                    <Border Style="{StaticResource SelectableGridItemBorderStyle}">
                        <Grid WidthRequest="120" HeightRequest="120">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <components:LibraryItemImageView
                                Grid.Row="0"
                                ImagePath="{Binding ImagePath}"
                                DefaultGlyph="{x:Static icons:SolidFont.Coins}"
                                Padding="20" />
                            <Label Text="{Binding Name}"
                                   Grid.Row="1"
                                   Style="{StaticResource TruncatedLabelStyle}"
                                   HorizontalOptions="Center"
                                   Margin="5"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <HorizontalStackLayout Grid.Row="2" Spacing="10" HorizontalOptions="Center" Margin="0,8">
            <components:FaIconButtonView Icon="{x:Static icons:SolidFont.Plus}"
                                         Command="{Binding CreateCommand}"
                                         Tooltip="{loc:Loc BtnAdd}"/>
            <components:FaIconButtonView Icon="{x:Static icons:SolidFont.Pen}"
                                         Command="{Binding EditCommand}"
                                         Tooltip="{loc:Loc BtnRename}"
                                         IsEnabled="{Binding SelectedEquipmentItem, Converter={StaticResource IsNotNullConverter}}"/>
            <components:FaIconButtonView Icon="{x:Static icons:SolidFont.Trash}"
                                         Command="{Binding DeleteCommand}"
                                         Tooltip="{loc:Loc BtnDelete}"
                                         IsEnabled="{Binding SelectedEquipmentItem, Converter={StaticResource IsNotNullConverter}}"/>
        </HorizontalStackLayout>

    </Grid>
</ContentView>
